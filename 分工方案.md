# AIVC Stage 2-3 分工方案

> 三人 × 两天（2026-08-07 ~ 2026-08-08）

---

## 0. 现状盘点与工作总量估算

### 已有资产（模块化重构已完成，代码质量良好）

| 文件 | 功能 | 状态 |
|------|------|:---:|
| [baseline/data.py](baseline/data.py) | 数据加载、预处理、split 划分 | ✅ |
| [baseline/features.py](baseline/features.py) | Phase 1 简易特征（one-hot + cyclic time） | ⚠️ 需升级 |
| [baseline/model.py](baseline/model.py) | Phase 1 `ConditionMLP`（简单 2 层 MLP） | ⚠️ 需重写 |
| [baseline/training.py](baseline/training.py) | mask-aware MSE + 基础训练循环 | ⚠️ 需升级 |
| [baseline/evaluation.py](baseline/evaluation.py) | 双基线 + 指标 + 提交生成 | ✅ 基础可用 |
| [run_baseline.py](run_baseline.py) | 端到端流水线脚本 | ✅ 基础可用 |

### 待建工作量

约 6-8 个新文件/模块重写，预计 **3 人 × 2 天 ≈ 48 人时**。

---

## 1. 工作包拆分与依赖拓扑

```
                    Day 1 上午（对齐接口+启动）            Day 1 下午 → Day 2
                    ════════════════════════            ═══════════════════

  Person A          [A1] 统计锚点实体表征               [A4] 时间注意力机制
  (特征+实验)        [A2] Hash编码 + 交叉特征            [A5] 消融实验框架
                    [A3] 特征工程流水线重构              [A6] Encoder消融 + 文档

  Person B          [B1] Encoder基础MLP               [B4] 蛋白共表达图 + GNN
  (架构核心)         [B2] 残差分解解码器(4 heads)        [B5] 全模型集成
                    [B3] 批次校准分支                   [B6] 架构消融 + Checkpoint

  Person C          [C1] Matched Control 工具函数       [C4] 训练循环升级
  (训练+分析)        [C2] FC Pearson loss              [C5] 全流程实验+对标
                    [C3] 残差L2 + 相关一致性loss        [C6] 结果整理+汇报材料
```

### 依赖关系

```
A(特征) ──→ B(模型) ──→ C(训练)
               │
               └──→ B(GNN) ──→ C(loss中的相关一致性)
```

- **A → B**：B 需要知道最终特征维度（约定 D_emb=256，B 先硬编码开发，A 交付后对接）
- **B → C**：C 需要模型的输出 dict 结构来写 loss（约定接口，见下）
- **B(GNN) → C**：蛋白相关一致性 loss 需要图结构（B 交付边列表后 C 对接）

---

## 2. 接口契约（所有人 Day 1 上午对齐确认）

这是三人能并行工作的关键。每人开始编码前必须确认以下契约。

### 契约 1：特征工程输出

```python
# baseline/features.py — Person A 交付
def build_condition_features(meta_df, encoders=None, fit_encoders=False):
    """
    返回:
        X: np.ndarray (N, D_emb) 其中 D_emb = 256
        encoders: dict (仅在 fit_encoders=True 时返回)
          - encoders["strain_prior"]: PCA model + mean vector
          - encoders["chem_anchor"]: PCA model + global fallback
          - encoders["hash_chemical"]: hash dimension
          - encoders["hash_plate"]: hash dimension
          - encoders["cat_encoders"]: {col: {value: idx}} for one-hot
          - encoders["_time_max"]: float
    """
```

### 契约 2：模型输出结构

```python
# baseline/model.py — Person B 交付
class AIVCModel(nn.Module):
    def forward(self, x):
        """
        x: (N, D_emb)

        返回 dict:
        {
            "y_pred":      (N, P)  最终预测 (after GNN + calibration)
            "y_raw":       (N, P)  残差求和 (before GNN, before calibration)
            "baseline":    (N, P)  baseline head 输出
            "delta_drug":  (N, P)  Δ_drug head 输出
            "delta_strain":(N, P)  Δ_strain head 输出
            "delta_context":(N, P) Δ_context head 输出
            "delta_cal":   (N, P)  calibration head 输出
        }
        其中 P = N_PROTEINS (约 4,422)
        满足: y_raw = baseline + delta_drug + delta_strain + delta_context
              y_pred = y_raw + delta_cal (如果 GNN 启用则还经过 GNN)
        """
```

### 契约 3：Loss 函数签名

```python
# baseline/losses.py — Person C 交付
def fc_pearson_loss(pred_dict, y_filled, mask, meta_batch, control_lookup, ...):
    """Fold Change Pearson 相关损失 → 标量"""

def residual_l2_loss(pred_dict):
    """残差 L2 正则: ||Δ_drug||² + ||Δ_strain||² + ||Δ_context||² → 标量"""

def correlation_consistency_loss(pred_dict, y_filled, mask, edge_index):
    """蛋白相关一致性: 预测相关矩阵 vs 训练集真实相关矩阵 → 标量"""
```

### 契约 4：配置文件格式

```python
# baseline/config.py — 三人共用
EXPERIMENT_CONFIG = {
    "exp_name": "residual_v1",
    "encoder": {
        "hash_dim_chemical": 32,
        "hash_dim_plate": 16,
        "strain_prior_pca_dim": 32,
        "chem_anchor_pca_dim": 64,
        "use_cross_features": True,
        "d_emb": 256,
    },
    "decoder": {
        "hidden": 256,
        "dropout": 0.1,
        "residual_decompose": True,  # False = 回退到端到端 MLP
    },
    "gnn": {
        "enabled": True,
        "k_neighbors": 5,
        "pearson_threshold": 0.7,
        "num_layers": 1,
    },
    "loss": {
        "mse_weight": 1.0,
        "fc_pearson_weight": 0.3,
        "residual_l2_weight": 0.01,
        "correlation_consistency_weight": 0.1,
    },
    "training": {
        "epochs": 100,
        "batch_size": 256,
        "lr": 1e-3,
        "weight_decay": 1e-5,
        "early_stopping_patience": 20,
    },
}
```

---

## 3. 逐人详细任务分解

### Person A：特征工程 + 消融实验

**角色定位**：负责所有「输入侧」工作，确保条件信息被最有效地编码。

#### Day 1（8月7日）

| 任务 | 文件 | 耗时 | 产出 |
|------|------|:---:|------|
| A1. 统计锚点实体表征 | `baseline/entity_representations.py` (新) | 3h | `StrainPriorEncoder` 类 + `ChemicalAnchorEncoder` 类 |
| A2. Hash编码 + 交叉特征 | 同上文件 | 1.5h | `HashEncoder` 类 + `CrossFeatureEncoder` 类 |
| A3. 特征工程流水线重构 | `baseline/features.py` (重写) | 2.5h | 新版 `build_condition_features()` 整合所有新特征，输出固定 dim=256 |

##### A1 细则——统计锚点

- `StrainPriorEncoder.fit(train_meta, y_log2, train_mask)`：
  1. 对每种菌株，取训练集中该菌株所有样本的蛋白均值 → (n_strains, P) 矩阵
  2. PCA 降维到 32 维
  3. 计算训练集全局蛋白均值作为 unseen fallback
- `StrainPriorEncoder.transform(meta_df)`：查表返回 (N, 32)，unseen → 全局均值
- `ChemicalAnchorEncoder` 同理：对每种化合物计算相对 Water/DMSO 的 Δ 均值 → PCA 到 64 维
  - **注意**：仅在训练集化合物上 fit，validation 中的 unseen 化合物 → 全局化合物 Δ 均值（理论接近零向量）
- **数据纪律**：所有 fit 操作仅用 `split_final=="train"` 的数据

##### A2 细则——Hash + 交叉

- Hash 编码：`hash(chemical_name) % hash_dim`，对 unseen 化合物友好（不需查表）
- 交叉特征：`strain × medium`（10 维）、`chemical × temperature`（最多 92 维，可压缩）
- 时间 sin-cos 编码已有（保留），增加周期参数化（周期=240min 而非简单 data max）

#### Day 2（8月8日）

| 任务 | 耗时 | 产出 |
|------|:---:|------|
| A4. 时间注意力机制 | 2h | 在 Δ_context 头中引入时间自注意力（与 Person B 协作） |
| A5. 消融实验框架 | 3h | `experiments/ablation_encoder.py`：分别消融 strain prior / chem anchor / hash / cross features |
| A6. Encoder 消融 + 文档 | 2h | 跑通 4 组消融，记录结果到共享文档 |

---

### Person B：模型架构 + GNN

**角色定位**：负责所有「模型内部」工作，是架构创新的核心执行者。

#### Day 1（8月7日）

| 任务 | 文件 | 耗时 | 产出 |
|------|------|:---:|------|
| B1. Encoder 基础 MLP | `baseline/model.py`（重写） | 1h | `ConditionEncoder` 类（输入 dim_in → 2-3 层 MLP → dim_emb=256） |
| B2. 残差分解解码器 | `baseline/decoder.py`（新） | 3h | `ResidualDecoder` 类（4 个独立 head + baseline head） |
| B3. 批次校准分支 | 同上或 `baseline/model.py` | 2h | `CalibrationHead` 类：以仪器/板 embedding 为输入输出 (P,) 加性偏移 |

##### B1 细则

- 将现有 `ConditionMLP` 的 2 层 MLP 结构保留为 encoder：
  `Linear(dim_in, 256) → ReLU → BN → Dropout → Linear(256, 256) → ReLU → BN → Dropout → Linear(256, 256)`
- 输出 256 维条件嵌入向量

##### B2 细则——残差分解解码器

- 5 个独立 head，每个都是 `Linear(256, 256) → ReLU → Dropout → Linear(256, P)` 的轻量 MLP
- `baseline_head`：输入 = encoder 输出的条件嵌入 → 输出共享基线
- `delta_drug_head`：输入 = 条件嵌入 + 化合物相关特征切片
- `delta_strain_head`：输入 = 条件嵌入 + 菌株相关特征切片
- `delta_context_head`：输入 = 条件嵌入 + 时间/温度/培养基特征切片（给 A4 留 hook）
- 每个 head 参数量约 `256*256 + 256*P ≈ 1.2M` × 5
  - **优化**：共享第一层 `Linear(256, 256)` 来减少参数（5 heads × 共享层 + 独立 projection），参数量约 1.5M
  - 总参数量控制在 < 5M

##### B3 细则——批次校准

- 输入：仪器 one-hot（7 类）+ 细胞板 hash 编码
- 输出：加性偏移向量 (P,)，对同一 plate 的所有样本共享
- 最终预测：`y_pred = (baseline + Δ_drug + Δ_strain + Δ_context) + δ_cal`

#### Day 2（8月8日）

| 任务 | 文件 | 耗时 | 产出 |
|------|------|:---:|------|
| B4. 蛋白共表达图 + GNN | `baseline/protein_graph.py`（新） | 3h | 图构建 + 1 层 GraphConv |
| B5. 全模型集成 | `baseline/model.py` | 2h | `AIVCModel` 类组合 Encoder + Decoder + GNN + Calibration |
| B6. 架构消融 | `experiments/ablation_arch.py` | 2h | 消融：残差分解 vs 端到端 / GNN vs 无 / 校准 vs 无 |

##### B4 细则——蛋白共表达图

- 从训练集 y_log2（5,920 样本 × 4,422 蛋白）→ 计算 Pearson 相关矩阵 → (4,422, 4,422)
- 对每个蛋白取 K=5 最高相关的邻居 → 构建 edge_index (2, E)
- 可选稀疏化：仅保留 |r| > 0.7 的边
- GNN 层：`torch_geometric.nn.GraphConv(P, P)`，1 层，初始化极小权重（接近恒等映射）
- **纪律**：图结构仅从 train 构建一次，不随训练更新

---

### Person C：损失函数 + 训练 + 全流程实验

**角色定位**：负责所有「优化目标」和「端到端验证」工作。

#### Day 1（8月7日）

| 任务 | 文件 | 耗时 | 产出 |
|------|------|:---:|------|
| C1. Matched Control 工具 | `baseline/evaluation.py`（扩展） | 1.5h | `compute_fold_change()` 函数——为每个 treatment 匹配对照并计算 FC |
| C2. FC Pearson loss | `baseline/losses.py`（新） | 2h | `fc_pearson_loss()`：预测 FC vs 真实 FC 的 Pearson r → 1-r |
| C3. 残差 L2 + 相关一致性 loss | `baseline/losses.py` | 2.5h | `residual_l2_loss()` + `correlation_consistency_loss()` |

##### C1 细则

- 复用 [evaluation.py](baseline/evaluation.py) 中已有的 `control_lookup` 和 `MATCH_KEYS`
- 对每个 treatment 样本：`fc_true = y_treat - y_control`（matched control 的蛋白向量）
- 对每个 treatment 样本：`fc_pred = pred_dict["delta_drug"]`（直接用 Δ_drug 头输出）
- 返回 `(fc_pred_tensor, fc_true_tensor, fc_mask)`——只在有匹配对照的样本上计算

##### C2 细则——FC Pearson loss

- 在 batch 内对所有可匹配到对照的 treatment 样本，摊平所有蛋白，计算预测 FC 与真实 FC 的 Pearson 相关系数
- `loss = 1.0 - pearson_r`
- **注意**：仅在训练集中可匹配到 Water/DMSO 对照的 treatment 样本上计算（约 4,000+ 样本）
- **当 batch 内无对照时此 loss 不计算，权重自动为 0**

##### C3 细则——辅助 loss

- 残差 L2：`||Δ_drug||²₂ + ||Δ_strain||²₂ + ||Δ_context||²₂`，促使各头输出趋近零（不放进去的信号不放）
- 相关一致性：在 batch 内计算预测的蛋白-蛋白相关矩阵，与训练集预计算的相关矩阵做 Frobenius 范数差

#### Day 2（8月8日）

| 任务 | 耗时 | 产出 |
|------|:---:|------|
| C4. 训练循环升级 | 2h | `baseline/training.py` 升级：支持多 loss 加权、loss 日志分离、early stopping 基于 val_both |
| C5. 全流程实验 | 4h | 跑通完整流水线：训练 → 验证 → 对标 → 提交；记录所有指标 |
| C6. 结果整理 + 文档 | 1h | 汇总三人结果到 `EXPERIMENT_LOG.md` |

##### C4 细则——训练循环改动

```python
# 原来
loss = mask_aware_mse(pred, y, mask)

# 新版
pred_dict = model(x)
loss_mse = mask_aware_mse(pred_dict["y_pred"], y, mask)
loss_fc = fc_pearson_loss(pred_dict, y, mask, meta_batch, ...)
loss_l2 = residual_l2_loss(pred_dict)
loss_corr = correlation_consistency_loss(pred_dict, y, mask, edge_index)
loss = w_mse * loss_mse + w_fc * loss_fc + w_l2 * loss_l2 + w_corr * loss_corr
```

- 每个 loss 分量独立记录到 history，便于后续诊断
- Early stopping：监控 `val_both Per-Protein R²` 而非 avg val loss

---

## 4. 两天时间线

### Day 1（8月7日）

| 时间段 | A（特征+实验） | B（架构） | C（训练+分析） |
|--------|:-------------|:---------|:-------------|
| **09:00-10:00** | **三人共同**：对齐接口契约、确认 config 结构、创建 git 分支 `feature/stage2-3` | | |
| **10:00-12:00** | A1：统计锚点类 | B1：Encoder 类 | C1：FC 工具函数 |
| **12:00-13:00** | 午饭 + 进度同步（15min） | | |
| **13:00-16:00** | A2：Hash + 交叉特征 | B2：残差分解解码器 | C2：FC Pearson loss |
| **16:00-18:00** | A3：特征流水线重构 | B3：批次校准分支 | C3：残差 L2 + 相关一致性 loss |
| **18:00-18:30** | **三人共同**：接口联调，确认 Encoder 输出能喂给 Decoder，模型输出 dict 结构符合 Loss 预期 | | |

### Day 2（8月8日）

| 时间段 | A（特征+实验） | B（架构） | C（训练+分析） |
|--------|:-------------|:---------|:-------------|
| **09:00-09:30** | **三人共同**：Day 1 进度回顾 + Day 2 任务确认 | | |
| **09:30-11:30** | A4：时间注意力（与 B 协作） | B4：蛋白图 + GNN | C4：训练循环升级 |
| **11:30-12:00** | 集成测试：全模型前向跑通（`pred_dict = model(dummy_input)` 不报错） | | |
| **12:00-13:00** | 午饭 | | |
| **13:00-14:00** | 首轮训练跑起来（所有人盯着 loss 曲线确保收敛） | | |
| **14:00-16:00** | A5+A6：Encoder 消融实验 | B5+B6：架构消融实验 | C5：全流程实验（8 split 对标） |
| **16:00-17:30** | 结果汇总 + 文档 | 结果汇总 + 文档 | C6：综合整理 + 汇报材料 |
| **17:30-18:00** | **三人共同**：最终同步——各组汇报实验结果、问题、结论 | | |

---

## 5. 并行度最大化策略

### Day 1 上午是关键窗口

接口契约确定后，三人可完全并行直到 Day 1 傍晚联调。

### 消除阻塞的刻意设计

| 潜在阻塞 | 缓解措施 |
|---------|---------|
| B 等 A 的 encoder 输出维度 | 约定 D_emb=256，B 用随机输入先开发 Decoder，A 交付后替换 |
| C 等 B 的 pred_dict 结构 | 用 `types.SimpleNamespace` mock 一个 fake pred_dict，C 先写 loss 逻辑 |
| C 等 B 的 edge_index | 蛋白图结构独立于模型，C 可以从 `protein_graph.py` 的构建函数直接拿 |
| 联调时集成失败 | 每个模块自带单元测试（给定已知输入 → 检查输出 shape 和值域） |

### 单元测试示例（每人自检）

```python
# Person A 自检
def test_encoder():
    X, enc = build_condition_features(meta_train, fit_encoders=True)
    assert X.shape == (len(meta_train), 256), f"Expected (N,256), got {X.shape}"
    assert not np.isnan(X).any(), "NaN in features"

# Person B 自检
def test_model():
    model = AIVCModel(dim_in=256, n_proteins=4422)
    x = torch.randn(32, 256)
    out = model(x)
    assert out["y_pred"].shape == (32, 4422)
    assert torch.allclose(
        out["y_raw"],
        out["baseline"] + out["delta_drug"] + out["delta_strain"] + out["delta_context"],
        atol=1e-5
    ), "Residual decomposition constraint violated"

# Person C 自检
def test_losses():
    fake_pred = {k: torch.randn(32, 4422) for k in [
        "y_pred", "baseline", "delta_drug", "delta_strain", "delta_context", "delta_cal"
    ]}
    loss_l2 = residual_l2_loss(fake_pred)
    assert loss_l2 > 0, "L2 loss should be positive"
```

---

## 6. 阶段验证里程碑

| 检查点 | 时间 | 通过标准 |
|--------|------|---------|
| **M1 接口对齐** | Day1 10:00 | 三人对契约无异议，contract 文档化 |
| **M2 模块自检** | Day1 18:00 | 每人模块独立通过 shape 和数值检查 |
| **M3 全模型跑通** | Day2 11:30 | `pred_dict = model(batch)` 不报错 |
| **M4 训练收敛** | Day2 14:00 | 前 20 epoch loss 下降，val_both Per-Protein R² > 0 |
| **M5 消融完成** | Day2 16:00 | 至少 6 组消融实验完成（Encoder × 2 + 架构 × 2 + Loss × 2） |
| **M6 结果记录** | Day2 18:00 | `EXPERIMENT_LOG.md` 记录所有实验配置和指标 |

---

## 7. 失败回退预案

| 如果…… | 则…… |
|---------|------|
| Day 1 傍晚联调发现架构跑不通 | Day 2 上午精简：先跑 **仅残差分解（无 GNN）+ 仅 MSE loss**，后续组件逐个加 |
| 残差分解 vs 端到端 MLP 消融结果为负 | 按方案文档 3.1.6 节回退：保留端到端 Decoder，残差分解降级为分析工具 |
| GNN 层 val 上无提升 | 跳过 GNN（方案文档已规划此回退），B 转去辅助 C 的消融实验 |
| FC loss 不收敛或指标下降 | FC loss 权重降为 0.1 或跳过一次训练，单独做 loss 消融 |
| 某人的模块严重 delay | 其余两人先用自己的模块 + mock 输入跑通训练，delay 的模块后续作为独立升级合入 |

---

## 核心原则

> **Day 1 建好所有积木 → Day 1 傍晚联调确保能拼起来 → Day 2 跑实验 + 记结果。**

宁可 Day 1 少做一个特性，不能 Day 2 还在 debug 接口不匹配。每个模块最多花 3 小时如果还调不通，立即切换到回退路径。

---

*文档版本：v1.0 | 撰写日期：2026-08-07 | 团队：AIVC 三人组*
