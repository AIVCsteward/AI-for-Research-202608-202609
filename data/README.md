# Data directory

The two raw proteome files are required for the baseline pipeline but are intentionally excluded from GitHub because of their size:

- `WAYB_WAYC_proteome_raw_train_val.csv`
- `WAYB_WAYC_proteome_raw_test.csv`

Keep them directly in this directory. The metadata CSV files are small enough to remain versioned.
