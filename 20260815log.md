# 2026-08-15 实验日志

> 分支：`lxy`
> 主题：官方评分口径核对 + 修正后续优先级

---

## 一、官方评分口径核对（CompetitorHandbook.pdf）

官方六模块中 **65% 权重明确用 PCC（尺度不变）**；ppr2（逐蛋白 R²）不在官方评分内。

| 模块 | 权重 | 计算方式 | 尺度敏感 |
|---|---|---|---|
| 绝对保真度 | 20% | 逐样本 corr / R² | ⚠️ 模糊 |
| 匹配对照 FC | 25% | PCC(Δ_pred, Δ_true) | 否 |
| 上下文均值残差 | 20% | PCC(Δ_pred−μ_ctx, Δ_true−μ_ctx) | 否 |
| 药物均值残差 | 20% | PCC(Δ_pred−μ_drug, Δ_true−μ_drug) | 否 |
| 双重未知/时间插值 | 10% | FC + 绝对保真度组合 | 部分 |
| 高效应 DEP | 5% | 方向准确率 / 高效应 PCC | 否 |

**关键判断**：

1. 官方评分脚本尚未公开（赛题解读：「最终指标组合以正式比赛规则为准」）。
2. FC(25%) + 残差(40%) + DEP方向(5%) = **70% 只在乎「Δ 排序与符号」，不在乎绝对幅度**。
3. 「绝对保真度 20%」官方写「corr / R²」——若最终用 R² 则尺度敏感，**待官方脚本确认**。
4. ppr2 非官方，可作诊断，但**不该被优化**（它崩是残差 loss 尺度不敏感的必然，不伤官方分）。

---

## 二、修正后的优先级

1. **FC oracle 上界**（判定 fc_pcc 是否可预测，决定方向）
2. **菌株/蛋白先验**（直接给排序信号：菌株→drug_residual，ESM→FC 蛋白维度）
3. **DEP 方向**
4. **尺度锚定**（降为保险项：仅保证预测落 log2 合理区间、不被 clamp 压到方差趋零）
5. leave-one-out μ、ppr2 —— 均搁置

---

## 三、AIVCModel 重跑（early stopping 修复）结论

见 [20260814log.md](20260814log.md) 第九节。核心：sample_corr 0.765 → 0.956，collapse 解决；ppr2 仍崩（非官方可不管）；fc_pcc 仍 ≈0（待测 oracle 上界）。

---

## 四、FC oracle 上界（判定 fc_pcc 是否可攻）

**方法**：训练集里同条件（菌株×培养基×温度×时间×药物）的生物学重复，用重复 A 的 Δ 预测重复 B 的 Δ，算 PCC。

| 重复类型 | n | mean | median |
|---|---|---|---|
| 跨板号（生物学重复） | 2498 | 0.113 | 0.057 |
| 同板号（技术重复） | 522 | 0.378 | 0.382 |

**结论**：FC 跨批次可复现性仅 ~0.11，模型 fc_pcc（0.02~0.11）已基本触及天花板。**fc_pcc ≈ 0 是 FC 信号的物理上限，不是方法 bug。** 应放弃 fc_pcc 优化，转投残差模块（40%）——待测残差 oracle。

---

## 五、残差 oracle（drug / ctx 残差的可复现性）

跨板号生物学重复下，各指标的可复现性：

| 指标 | mean | median | 说明 |
|---|---|---|---|
| 原始 FC (Δ) | 0.113 | 0.057 | 干净 |
| drug 残差 (Δ−μ_drug) | 0.053 | 0.041 | 干净（μ_drug 样本多） |
| ctx 残差 (Δ−μ_ctx) | ≈ 0 | — | LOO=+0.34 / 含自身=−0.47，均虚假相关 |

**统计陷阱**：ctx 残差用 leave-one-out μ 会人为制造 +1/(n−1) 正相关、用含自身 μ 制造 −1/(n−1) 负相关。两者方向相反、绝对值接近（+0.34 / −0.47），真实可复现性夹在中间 ≈ 0。

**核心结论**：官方三个 FC 类指标（FC 25% + drug 残差 20% + ctx 残差 20% = **65% 权重**）跨批次可复现性都极低（0~0.11）。蛋白质组扰动响应被批次效应 + 测量噪声淹没，**这 65% 分数在数据层面注定接近随机，任何模型都无法从条件特征预测**。

**真正可攻的战场**：绝对保真度 20%（模型 0.95，天花板 matched control 0.985）+ 时间插值 10% + DEP 方向 5%。

---

## 六、RMSE 参考（诊断指标，非官方）

官方六模块无 RMSE，但赛题解读基线表用「log2 RMSE」诊断绝对误差。各模型训练期 RMSE（best_val_loss 开根号，近似）：

| 模型 | 组 | best_val_loss(MSE) | RMSE(≈√MSE) |
|---|---|---|---|
| ConditionMLP MSE-only | full | 0.338 | 0.581 |
| ConditionMLP MSE-only | no_chemical_structure | 0.357 | 0.598 |
| ConditionMLP residual | full | 0.542 | 0.736 |
| ConditionMLP residual | no_chemical_structure | 0.541 | 0.736 |
| AIVC 崩掉 | full | 0.731 | 0.855 |
| AIVC 崩掉 | no_chemical_structure | 0.880 | 0.938 |
| AIVC 修复 | full | 0.553 | 0.744 |
| AIVC 修复 | no_chemical_structure | 0.542 | 0.736 |

参考基线（赛题解读）：protein mean RMSE≈0.87~1.0，matched control RMSE≈0.38~0.43。

**解读**：加残差 loss 后 RMSE 从 ~0.58 升到 ~0.74（残差 loss 牺牲绝对误差换排序，印证「残差 vs 绝对拟合」对立）；AIVC 修复版 RMSE 0.74 与 ConditionMLP residual 持平。

---

## 七、去噪实验：能否抬高 FC oracle 上界？

验证「信号被噪音淹没」假说（跨板号生物学重复 oracle，baseline mean=0.113）：

| 实验 | 方法 | oracle mean | 提升 |
|---|---|---|---|
| **A 信噪比筛选** | top-500 高 SNR 蛋白 | 0.215 | +90% |
| A 信噪比筛选 | top-1000 | 0.171 | +51% |
| **B 共识去噪** | leave-one-out 中位数 | 0.173 | +53% |
| **C 批次校正** | plate 级去偏 | 0.113 | **0%（无效）** |
| **D 低秩分解** | SVD k=10 | **0.286** | **+153%** |
| D 低秩分解 | SVD k=50 | 0.239 | +111% |
| D 低秩分解 | SVD k=100 | 0.216 | +91% |

SNR 分布：median 0.34，约 **20% 蛋白 SNR > 0.5**。

**核心结论**：

1. **信号确实存在，被噪音淹没**——去噪能大幅抬高 FC 可复现性（低秩 +153%、信噪比 +90%），**推翻第五节「65% 注定随机」的悲观判断**。
2. **低秩分解最有效**（k=10 → 0.286），说明真实效应是低秩结构、噪音是满秩的，可被 SVD 截断分离。
3. **批次校正无效（+0%）**——matched control 已通过同 plate 相减抵消 plate 级批次效应；问题不在批次，而在蛋白级随机噪音 + 满秩噪音。
4. **下一步**：把「低秩去噪」和「高 SNR 蛋白子集」整合进训练，fc_pcc 有望从 ~0.02 抬到 0.2+。
